// English language pack.
export default {
  app: {
    name: 'Mantou',
    tagline: 'Self-hosted networking toolbox',
  },
  common: {
    save: 'Save',
    maxEntries: 'up to {n} entries',
    cancel: 'Cancel',
    confirm: 'Confirm',
    delete: 'Delete',
    edit: 'Edit',
    add: 'Add',
    enable: 'Enable',
    disabled: 'Disabled',
    enabled: 'Enabled',
    status: 'Status',
    name: 'Name',
    actions: 'Actions',
    yes: 'Yes',
    no: 'No',
    loading: 'Loading…',
    empty: 'No data',
    success: 'Success',
    failed: 'Failed',
    saved: 'Saved',
    deleted: 'Deleted',
    confirmDelete: 'Are you sure you want to delete this?',
    run: 'Run now',
    test: 'Test',
    close: 'Close',
    reset: 'Reset',
    apply: 'Apply',
    all: 'All',
    unnamed: 'Untitled',
    optional: 'Optional',
    advanced: 'Advanced',
    import: 'Import',
    export: 'Export',
    none: 'None',
    manage: 'Manage',
    more: 'More',
    basic: 'Basic',
    saveFailed: 'Save failed',
    loadFailed: 'Load failed',
    required: 'This field is required',
    refresh: 'Refresh',
  },
  account: {
    title: 'Account & password',
    username: 'Login username',
    oldPassword: 'Current password',
    newPassword: 'New password',
    newPasswordHint: 'Leave blank to keep the current password',
    confirmNew: 'Confirm new password',
    needOldPassword: 'Enter your current password to confirm',
    nothingToChange: 'Nothing to change',
    passwordMismatch: 'New passwords do not match',
    usernameChangedRelogin: 'Login username changed — please sign in again with the new username',
  },
  nav: {
    overview: 'Overview',
    ddns: 'Dynamic DNS (DDNS)',
    webservice: 'Web Services',
    forward: 'Port Forwarding',
    wol: 'Wake-on-LAN',
    cron: 'Scheduled Tasks',
    mroute: 'Message Routing',
    cert: 'SSL/TLS Certificates',
    cred: 'DNS Provider Credentials',
    settings: 'Settings',
    about: 'About',
    logout: 'Sign out',
  },
  login: {
    title: 'Sign in to Mantou',
    username: 'Username',
    password: 'Password',
    submit: 'Sign in',
    placeholderUser: 'Enter username',
    placeholderPass: 'Enter password',
    failed: 'Invalid username or password',
    noSession:
      'Credentials accepted, but the browser did not store the session, so the panel cannot be opened. Check whether cookies are blocked for this site (private mode, privacy extensions, “block all cookies”); if it still fails, clear this site’s cookies and retry.',
  },
  setup: {
    title: 'Set up Mantou',
    subtitle: 'First run — create your administrator account',
    username: 'Admin username',
    password: 'Password',
    confirmPassword: 'Confirm password',
    hintUser: 'At least 3 characters',
    hintPass: 'At least 6 characters',
    mismatch: 'Passwords do not match',
    submit: 'Create and continue',
    done: 'Setup complete',
  },
  overview: {
    title: 'Overview',
    subtitle: 'System status and module summary',
    sysCpu: 'System CPU',
    procCpu: 'Process CPU',
    sysMem: 'System memory',
    sysMemScopeContainer: 'Container view',
    sysMemScopeHost: 'This machine',
    memUsed: 'Memory usage',
    memScopeContainer: 'Whole container',
    memScopeProcess: 'This app',
    net: 'Network throughput',
    down: 'Down',
    up: 'Up',
    uptime: 'Uptime',
    startedAt: 'Startup date',
    version: 'Version',
    recvTotal: 'Total received',
    sentTotal: 'Total sent',
    modules: 'Module status',
    total: 'Total',
    active: 'Active',
    healthy: 'Healthy',
    unhealthy: 'Degraded',
    realtime: 'Live metrics',
    logs: 'Application logs',
    noLogs: 'No logs yet',
    checkUpdate: 'Check for updates',
    checking: 'Checking…',
    updateTitle: 'Online update',
    update: 'Update',
    upToDate: 'You are on the latest version',
    updateAvailable: 'New version {v} available',
    updateDisabled: 'Online update check is not enabled yet',
    networkError: 'Cannot reach network',
    viewRelease: 'Go to download',
    modName: {
      ddns: 'Dynamic DNS (DDNS)',
      webservice: 'Web Services',
      forward: 'Port Forwarding',
      wol: 'Wake-on-LAN',
      cron: 'Scheduled Tasks',
      webhook: 'Message Routing · Inbound',
      notify: 'Message Routing · Outbound',
      cert: 'Certificates',
    },
  },
  cred: {
    title: 'DNS Provider Credentials',
    subtitle: 'Central API credentials for DNS providers, shared by Dynamic DNS and certificate issuance',
    credName: 'Credential name',
    provider: 'Provider',
    fields: 'Credential fields',
    emptyHint: 'No credentials yet — click “Add” in the top right',
    providerName: {
      cloudflare: 'Cloudflare',
      godaddy: 'GoDaddy',
      aliyun: 'Alibaba Cloud',
      tencent: 'Tencent Cloud',
      baidu: 'Baidu AI Cloud',
    },
    fieldLabel: {
      apiToken: 'API Token',
      apiKey: 'API Key',
      apiSecret: 'API Secret',
      accessKeyId: 'AccessKey ID',
      accessKeySecret: 'AccessKey Secret',
      accessKey: 'Access Key',
      secretKey: 'Secret Key',
      secretId: 'Secret ID',
    },
  },
  ddns: {
    title: 'Dynamic DNS (DDNS)',
    subtitle: 'Detect public IP on a schedule and update DNS records',
    ruleName: 'Rule name',
    stack: 'IP stack',
    interval: 'Interval (s)',
    source: 'Public IP source',
    sourceType: {
      public: 'Public (auto)',
      interface: 'Local interface',
      url: 'HTTP endpoint',
    },
    publicHint: 'Automatically probes your public IP from several public services — no configuration required (recommended).',
    iface: 'Interface',
    url: 'Endpoint URL',
    regex: 'Extract regex',
    targets: 'Update targets',
    provider: 'Provider',
    credential: 'DNS provider',
    domain: 'Domain',
    subdomain: 'Host record',
    subdomains: 'Host records (subdomains)',
    subdomainsPlaceholder: 'Type a host record and press Enter; add several (e.g. home, nas)',
    subdomainsHint: 'Multiple domains or IPs are supported — type one and press Enter to add each',
    allowRoot: 'Allow root domain',
    allowRootHint: "Higher risk: rewrites the whole apex domain ({'@'}) record. Off by default",
    recordType: 'Record type',
    ttl: 'TTL (s)',
    ttlUnit: 's',
    ttlAuto: 'Auto (provider decides)',
    ttlAutoHintCloudflare: 'Cloudflare auto-tunes cache duration by real traffic for fastest propagation',
    ttlAutoHintOther: 'Non-Cloudflare providers have no adaptive TTL; the provider minimum (~600s) is used',
    line: 'Resolution line',
    lineHint: 'Optional, e.g. “default”',
    addTarget: 'Add target',
    noCredential: 'No credentials yet — add one on the DNS Provider Credentials page first',
    subdomainExample:
      'Dynamic DNS only updates the IP of host records (subdomains), not the whole domain. For example, domain example.com + host record home updates home.example.com; you can add several host records at once (e.g. home, nas). The root domain is riskier and is not allowed by default — turn on “Allow root domain” only if you really need to update the apex record.',
    lastIp: 'Last IP',
    lastStatus: 'Last status',
    lastUpdate: 'Updated at',
    lastUpdateCol: 'Last updated',
    runNow: 'Update now',
    requiredHint: 'Fill in the DNS provider and domain, and add at least one host record (or enable root domain) before saving',
  },
  webservice: {
    title: 'Web Services',
    subtitle: 'Reverse proxy, static sites and redirects with HTTPS',
    svcName: 'Service name',
    type: 'Type',
    typeProxy: 'Reverse proxy',
    typeStatic: 'Static site',
    typeRedirect: 'Redirect / rewrite',
    listens: 'Listeners',
    domains: 'Domains',
    upstreams: 'Upstreams',
    root: 'Site root',
    gzip: 'Response compression (gzip)',
    gzipHint:
      'Compresses responses based on their content type: text-like payloads (HTML / CSS / JS / JSON / SVG / fonts) are gzipped before sending, ' +
      'which typically cuts 60%-80% of the bytes on the wire for a faster first paint and lower bandwidth. ' +
      'Images, video and already-compressed archives (zip / gz / woff2, ...) gain nothing from compression, so they are sent as-is and keep using the kernel zero-copy (sendfile) path with no extra CPU cost. ' +
      'Responses under 1 KB and range (resumable download) requests skip compression automatically. ' +
      'This switch applies to static sites only; reverse-proxy children are untouched (the upstream may already compress, and re-compressing wastes CPU and breaks streaming pushes).',
    dirList: 'Directory listing',
    dirListHint:
      'When off (the default), requesting a directory that has no index file simply returns 404; when on, the files in that directory are listed. ' +
      'Off by default because a site root is often just a project folder, and a listing hands visitors your backup files, scratch files and folder structure — ' +
      'when all they need are the pages you meant to publish. ' +
      'Dot-prefixed files and folders (.git, .env, ...) are never served either way, with .well-known as the one exception.',
    tls: 'Enable TLS',
    addListen: 'Add listener',
    redirectHttps: 'Force HTTPS',
    proxyOptions: 'Security settings',
    ipFilter: 'IP filter',
    ipWhitelist: 'IP allowlist',
    ipBlacklist: 'IP denylist',
    ipList: 'IP list',
    ipListPlaceholder: 'Single, multiple or ranges — press Enter to add (e.g. 1.2.3.4, 10.0.0.0/24, 192.168.1.1-192.168.1.20)',
    ipFilterHint: 'When on, the chosen mode applies: allowlist permits only listed IPs; denylist blocks listed IPs (higher priority)',
    rateLimit: 'Rate limit',
    rateLimitHint: 'When enabled, limits requests per second per client (token bucket); exceeded requests get 429. Protects the backend from a single client abusing the API. 0 = unlimited.',
    ipAllow: 'IP allowlist',
    ipDeny: 'IP denylist',
    ipAllowHint: 'Only IPs / CIDRs in the list may access; leave empty to allow all (e.g. 1.2.3.4 or 10.0.0.0/24)',
    ipDenyHint: 'IPs / CIDRs in the list are refused (takes priority over the allowlist)',
    insecureSkipVerify: 'Skip backend TLS verification',
    preserveHost: 'Pass original Host header',
    preserveHostHint: 'On: forward the incoming Host; off: rewrite to the target host',
    accessLog: 'Record access logs',
    accessLogLimit: 'Show recent entries',
    redirectTarget: 'Target URL',
    redirectCode: 'Status code',
    keepPath: 'Keep original path',
    keepQuery: 'Keep query string',
    redirectHint: 'e.g. https://example.com',
    stepListen: 'Listen port',
    stepFront: 'Frontend address (domain / IP)',
    stepBack: 'Backend address (domain / IP)',
    frontHint:
      'The domain or IP used to reach this service; leave blank to match all requests arriving on this port. Matching is exact — wildcards such as *.example.com are not supported here (they only belong in a certificate); list every domain that will actually be visited.',
    backHint: 'The backend target where traffic is ultimately forwarded.',
    tlsMinVersion: 'Minimum TLS version',
    tlsVersionAuto: 'Default (TLS 1.2)',
    port: 'Listen port',
    ipFamily: 'IP family',
    probeInterval: 'Probe interval (s)',
    probeIntervalHint: 'Backend reachability probe period for all rules under this parent; choose 15 / 30 / 60 / 300 seconds, default 60s.',
    familyBoth: 'All (dual stack)',
    familyV4: 'IPv4 only',
    familyV6: 'IPv6 only',
    hsts: 'Enable HSTS',
    basicAuth: 'Access authentication (username & password)',
    basicAuthUser: 'Access username',
    basicAuthPass: 'Access password',
    basicAuthPassSet: 'Already set — leave empty to keep it',
    basicAuthPassNew: 'Enter the access password',
    basicAuthHint:
      'When enabled, every request to this rule (all paths included) must first pass the browser username/password prompt before being forwarded to the backend. ' +
      "It is an extra door in front of the backend's own login, meant for internal dashboards, staging sites and anything that should not be publicly reachable. " +
      'The password is stored hashed and never shown again; simply set a new one if you forget it. Verification results are cached for a few minutes, so a password change may take a moment (or a browser restart) to take effect. ' +
      'This feature requires TLS on this rule: Basic credentials sent over plain HTTP are effectively public, so they must be carried by HTTPS. ' +
      'Enabling the rate limit above is recommended as well, so nobody can burn CPU with a flood of wrong passwords.',
    basicAuthNeedCred: 'A rule with access authentication enabled must have both a username and a password',
    redirectHttpsLocked:
      'TLS is enabled, so Force HTTPS is turned on automatically and cannot be switched off: a rule that already serves HTTPS should not keep accepting plaintext.',
    redirectHttpsHint:
      'Redirects plaintext HTTP requests for this rule to HTTPS. To get the classic "port 80 jumps to 443", enable this on the parent listening on port 80 (whose rules do not use TLS).',
    trustProxyHeaders: 'Trust upstream proxy protocol headers',
    trustProxyHeadersHint:
      'Only needed when this rule really sits behind an external TLS-terminating proxy (Cloudflare, nginx, ...): ' +
      'with it on, an https declared by the upstream through X-Forwarded-Proto or CF-Visitor counts as "this request already arrived over HTTPS", ' +
      'so the redirect above no longer fires and the HSTS header is sent. ' +
      'When off (the default) only a real TLS connection to this machine counts — anyone can put those two headers in a request, ' +
      'and trusting them means letting the visitor decide whether the HTTPS redirect applies.',
    children: 'Rules',
    child: 'Rule',
    addChild: 'Add rule',
    childrenCap: 'up to {n} rules',
    childrenFull: 'Rule limit reached ({n}); remove one first',
    childNote: 'Note',
    frontAddr: 'Frontend',
    backAddr: 'Backend',
    connections: 'Connections',
    viewLogs: 'Logs',
    logsTitle: 'Connection logs',
    noLogs: 'No connections recorded yet',
    dupParent: 'A parent with the same port + IP family already exists; add rules under it instead',
    needChild: 'At least one rule is required',
    mixedProtocol: 'Rules under the same parent cannot mix HTTP and HTTPS; use the same TLS setting',
    parentHint: 'Port and IP family form the parent rule; each port + IP family allows only one parent, which can hold multiple rules.',
    parentDisabledHint: 'Parent is off; all its rules are disabled.',
    childSummary: 'Rule',
    logTime: 'Time',
    logReq: 'Action',
    logStatus: 'Result',
    logStatusSuccess: 'Success',
    logStatusDisconnect: 'Disconnected',
    logStatusError: 'Error',
    logStatusDenied: 'Denied',
    logReason: 'Backend status',
    logBackendOk: 'Connected',
    logRemote: 'Source IP',
    logDur: 'Duration (ms)',
    linkStatus: 'Link status',
    linkNormal: 'OK',
    linkFail: 'Access error',
    linkUnreachable: 'Unreachable',
    linkIdle: 'No traffic',
  },
  forward: {
    title: 'Port Forwarding',
    subtitle: 'TCP/UDP forwarding with IPv4/IPv6 translation',
    ruleName: 'Rule name',
    protocol: 'Protocol',
    listenPort: 'Listen port',
    targetHost: 'Target host',
    targetPort: 'Target port',
    targetHint: 'IPv4 / IPv6 / domain',
    family: 'Address family',
    familyDual: 'Dual stack',
    familyV4: 'IPv4 only',
    familyV6: 'IPv6 only',
    bind: 'Bind address',
    bindHint: 'Leave empty = listen on all interfaces (0.0.0.0/::); set 127.0.0.1 for local-only, or a specific IP for that NIC only',
    lastError: 'Last error',
    runStatus: 'Run status',
    normal: 'OK',
    enableRange: 'Port range (multiple ports)',
    listenPortStart: 'Listen port start',
    listenPortEnd: 'Listen port end',
    targetPortStart: 'Target port start',
    rangeHint: 'Listens on the contiguous range [start, end]; target ports increase from “Target port start” by the same offset (e.g. listen 8000-8002 → target 9000-9002).',
    rangeHintSame: 'Listens on the contiguous range [start, end]; every port forwards to the same “Target port” (e.g. listen 8000-8002 → all to 9000).',
    targetMode: 'Target port mapping',
    targetModeIncrement: 'Increment',
    targetModeSame: 'Many-to-one',
  },
  wol: {
    title: 'Wake-on-LAN',
    subtitle: 'Wake LAN devices remotely via magic packets, with scheduling',
    devName: 'Device name',
    mac: 'MAC address',
    macHint:
      'Both AA:BB:CC:DD:EE:FF and AA-BB-CC-DD-EE-FF are accepted, as is AABBCCDDEEFF without separators; case-insensitive. It is stored and shown in upper-case colon form.',
    broadcast: 'Broadcast address',
    broadcastHint:
      'Three forms are accepted: leave it empty to broadcast through every interface automatically; enter a subnet broadcast address (e.g. 192.168.1.255); or enter the target device\'s own LAN IP to send a unicast packet (e.g. 192.168.1.100 — any 192.168.x.x style address). A NIC\'s wake engine matches on the MAC inside the frame and does not care whether the frame was unicast or broadcast. Unicast is the way to go when mantou runs in a container that shares no layer-2 broadcast domain with the target. Note that a powered-off machine no longer answers ARP, so add a static ARP entry for that IP and MAC on your router — otherwise the packet is dropped for want of a MAC. Only private addresses are accepted; public ones are rejected, as magic packets are not routed.',
    port: 'Port',
    iface: 'Source interface',
    ifaceHint:
      'Leave empty for automatic: magic packets go out only through private physical interfaces. Virtual ones (container bridges, VMs, tunnels) and public ones are skipped — broadcasting there cannot wake anything, and it leaks the target MAC to container peers or datacenter neighbours. Pick an interface here if automatic selection is wrong on a multi-homed host.',
    ifaceAutoOption: 'Automatic (recommended)',
    ifaceAutoUses: 'Automatic mode sends through: {list}',
    ifaceNoAuto:
      'This host has no private interface usable for waking (only virtual and/or public ones), so automatic mode will fail. Select one explicitly above if you really need it.',
    ifaceNoneFound: 'Could not read the interface list; you can type a name manually (e.g. eth0).',
    ifaceVirtual: 'virtual',
    ifacePublic: 'public',
    ifaceAuto: 'used by auto',
    manyDevices:
      '{total} devices configured, {scheduled} of them with scheduled wake enabled. Each scheduled device keeps a background task running and writes its state back on every tick — the more devices and the shorter the interval, the heavier the panel gets. Consider turning off schedules you no longer need.',
    note: 'Note',
    lastWake: 'Last wake',
    wakeCount: 'Wake count',
    wake: 'Wake',
    waking: 'Sending magic packet…',
    schedule: 'Scheduled wake',
    scheduleEnabled: 'Enable scheduled wake',
    calendarEnabled: 'Limit by calendar',
    calendar: 'Execution date range',
    calendarTo: 'to',
    calendarStart: 'Start date',
    calendarEnd: 'End date',
    mode: 'Trigger mode',
    modeFixed: 'Fixed time',
    modeRange: 'Time range',
    time: 'Time',
    start: 'Start time',
    end: 'End time',
    count: 'Packets (within 1s)',
    interval: 'Send interval (s)',
    scheduleOff: 'Off',
    listFixed: '{time} ×{count}',
    listRange: '{start}–{end} every {sec}s',
    hintFixed:
      'Fires once a day at this single point in time, sending that many magic packets spread evenly across that one second. The extra packets only guard against occasional loss — the device still wakes just once; set 1 to send a single packet. This mode does not use the send interval.',
    hintRange:
      'Every day, starting at the start time, sends one magic packet every N seconds until the end time is passed; the start time itself is the first send. Useful when the device may only be powered during that window, or needs repeated attempts. This mode does not use the packet count.',
    rangePreview: 'With the current settings: {n} magic packets per day, from {start} to {end}.',
    rangePreviewDense:
      'A very small interval generates a lot of broadcast traffic; 30 seconds or more is recommended.',
    rangeEndBeforeStart:
      'The end time is not after the start time, so only one packet is sent per day, at the start time. To cover a window that crosses midnight (e.g. 22:00 to 06:00 the next day), split it into two devices: 22:00–23:59 and 00:00–06:00.',
  },
  cron: {
    title: 'Scheduled Tasks',
    subtitle: 'Pick a trigger and the cron schedule is generated for you',
    taskName: 'Task name',
    scheduleType: 'Trigger',
    type: {
      minutely: 'Every minute',
      hourly: 'Hourly',
      daily: 'Daily',
      weekly: 'Weekly',
      monthly: 'Monthly',
      interval: 'Fixed interval',
      custom: 'Custom expression',
    },
    atMinute: 'At minute',
    atTime: 'At time',
    everyMinutes: 'Every (minutes)',
    everyN: 'Run every',
    unit: { minutes: 'minutes', hours: 'hours' },
    weekday: 'Weekday',
    day: 'Day of month',
    expr: 'Cron expression',
    customExpr: 'Custom cron expression',
    generated: 'Generated expression',
    weekdayNames: ['Sun', 'Mon', 'Tue', 'Wed', 'Thu', 'Fri', 'Sat'],
    action: 'Action',
    actionType: {
      ddnsRefresh: 'Refresh Dynamic DNS',
      wolWake: 'Wake device',
      certRenew: 'Renew certificates',
      http: 'HTTP request',
    },
    target: 'Target',
    url: 'URL',
    method: 'Method',
    timeout: 'Timeout (seconds)',
    timeoutHint: '0 uses the default cap (HTTP 30s, renew 10m)',
    lastRun: 'Last run',
    nextRun: 'Next run',
    runNow: 'Run now',
    runOk: 'Task triggered',
    runFail: 'Run failed',
    exprHint: 'Format: min hour day month weekday, e.g. 0 3 * * *',
  },
  cert: {
    title: 'SSL/TLS Certificates',
    subtitle: 'Import PEM, load from disk path, or auto-issue via ACME',
    certName: 'Certificate name',
    domains: 'Domains (one or more)',
    domainsPlaceholder: 'Add one at a time: type a domain and press Enter, e.g. example.com',
    domainsHint: 'Add each domain separately (press Enter to confirm); wildcard *.example.com is supported. You may also write "a.com, *.a.com" together — it will be split automatically.',
    method: 'Method',
    methodFile: 'Import PEM',
    methodPath: 'Disk path',
    methodAcme: 'ACME auto-issue',
    certPath: 'Certificate file path',
    keyPath: 'Private key file path',
    pathHint: 'Absolute paths to the certificate and key files on the server',
    path: 'Certificate paths',
    certStatus: 'Certificate status',
    export: 'Export',
    exported: 'Certificate exported',
    exportedWithKey: 'Exported certificate and private key as two files',
    exportedNoKey: 'Certificate exported, but the private key could not be read (certificate only)',
    dns01Hint: 'Issued via DNS-01 validation using the selected DNS credential; wildcard certificates (*.example.com) are supported.',
    credential: 'DNS provider',
    acme: 'ACME account',
    autoRenew: 'Auto renew',
    renewBefore: 'Renew before (days)',
    execTime: 'Execution time',
    renewTimeHint: 'Check daily at this time and renew certificates nearing expiration (e.g. 09:00)',
    notAfter: 'Expires',
    nextRenew: 'Next renewal',
    renewInAdvance: 'Renew {n} days in advance',
    remainingDays: '{n} days remaining',
    expiredDays: 'Expired {n} days ago',
    expired: 'Expired',
    enable: 'Enable',
    disable: 'Disable',
    disableConfirm: 'After disabling, this certificate will no longer be used for TLS listeners. Disable it?',
    inUseCannotDisable: 'This certificate is referenced by panel HTTPS and cannot be disabled',
    issue: 'Issue / Renew',
    importPem: 'Import PEM',
    import: 'Import certificate',
    fileHint: 'After saving, click “Import PEM” in the list and pick the certificate and key files (pasting also works)',
    certPem: 'Certificate PEM',
    keyPem: 'Private key PEM',
    chooseFile: 'Choose file',
    orPaste: 'or paste below',
    notPemFile: 'That file does not look like PEM (no -----BEGIN header); please pick another',
    importPairHint: 'Both certificate and private key are required. The .pem and .key that Export downloads map to these two boxes.',
    manageAcme: 'ACME accounts',
    acmeTitle: 'ACME account management',
    accountName: 'Account name',
    ca: 'CA',
    email: 'Email',
    eabKid: 'EAB KID',
    eabHmac: 'EAB HMAC',
    eabHint: 'ZeroSSL / Buypass require EAB credentials; leave blank for Let’s Encrypt',
    caName: {
      letsencrypt: 'Let’s Encrypt',
      zerossl: 'ZeroSSL',
      buypass: 'Buypass',
    },
    noAccount: 'No ACME account yet — create one under “ACME accounts” first',
    status: {
      valid: 'Valid',
      expired: 'Expired',
      missing: 'Missing',
      issuing: 'Issuing',
      renewing: 'Renewing',
      pendingIssue: 'Issue pending',
      pendingRenew: 'Renew pending',
      issueSuccess: 'Issued',
      renewSuccess: 'Renewed',
      issueFailed: 'Issuance failed',
      renewFailed: 'Renewal failed',
      interrupted: 'Interrupted (retry)',
    },
  },
  settings: {
    title: 'Settings',
    subtitle: 'Panel, appearance, logs, account and backup',
    tabGeneral: 'General',
    tabAppearance: 'Appearance',
    tabAccount: 'Account',
    tabLog: 'Logs',
    tabBackup: 'Backup & restore',
    tabAbout: 'About',
    tabSecurity: 'Login security',
    tabUpdate: 'Online update',
    tabRestart: 'Restart',
    // Restart: an immediate restart replaces the whole process; the schedule picks times by weekday / calendar / interval.
    restartNow: 'Restart now',
    restartNowDesc: 'Restarting replaces the whole process. Existing port-forwarding connections drop and any running scheduled task is interrupted. Configuration and runtime state are written to disk first, so nothing is lost.',
    restartNowConfirm: 'Restart the program now? Existing connections will drop and the panel should be back within a few seconds.',
    restartNowCountdown: 'Restart started; this page will reload automatically in {n} seconds.',
    restartSchedule: 'Scheduled restart',
    restartScheduleEnabled: 'Scheduled restart',
    restartScheduleHint: 'Settings below are kept while this is off',
    restartMode: 'Schedule by',
    restartModeWeekly: 'Weekday',
    restartModeDates: 'Calendar',
    restartModeInterval: 'Every N days',
    restartWeekdays: 'Weekdays',
    restartWeekdayRequired: 'Pick at least one weekday',
    restartDates: 'Dates',
    restartDatesPlaceholder: 'Pick dates on the calendar',
    restartDatesHint: 'Multiple dates allowed, up to 60; dates in the past no longer fire',
    restartDatesRequired: 'Pick at least one date',
    restartEveryDays: 'Interval (days)',
    restartEveryDaysHint: 'Counted from the start date',
    restartStartDate: 'Start date',
    restartStartDatePlaceholder: 'Pick a start date',
    restartStartDateHint: 'The interval is anchored to this date, so restart days stay fixed instead of drifting',
    restartStartDateRequired: 'A start date is required for the interval mode',
    restartAt: 'Time',
    restartAtHint: 'Local time, to the minute',
    restartNextRun: 'Next run',
    restartNoNextRun: 'None',
    restartLastRun: 'Last run: ',
    // Online-update settings: self-hosted manifest takes precedence over GitHub; either can be left blank to use defaults.
    updatePriorityHint: 'Detection priority: self-hosted manifest > GitHub repository. Either field may be left blank to use the default (default repo: ovoene/Mantou). Prefer HTTPS for both the manifest and download URLs, and configure the "Update package signing key" below to protect against a tampered update source.',
    updateManifestUrl: 'Self-hosted manifest URL',
    updateManifestUrlHint: 'URL returning JSON with version / url / description. When set, takes precedence over GitHub detection (suitable for private deployments); leave empty to fall back to GitHub.',
    updateReleaseUrl: 'Release page URL',
    updateReleaseUrlHint: 'Page opened by the Update button in About when a new version is detected. Leave empty to fall back to the manifest or GitHub releases.',
    updateGithubRepo: 'GitHub repository',
    updateGithubRepoHint: 'owner/name form (e.g., ovoene/Mantou), or a full https URL. Leave empty for the default ovoene/Mantou; only takes effect when no self-hosted manifest is configured.',
    updateSignKey: 'Self-update signature public key',
    updateSignKeyHint: 'Ed25519 public key (base64-encoded 32 bytes). When set, an uploaded update package must include a matching .sig signature file that verifies before it is applied. When left empty, whether packages are accepted at all is decided by the next option.',
    updateAllowUnsigned: 'Accept unsigned update packages',
    updateAllowUnsignedHint: 'With no public key configured, uploaded packages are applied without signature verification. While this is off and the key is empty, update packages are not accepted — uploading one means the panel runs a new binary, and without a signature there is no way to tell where it came from.',
    language: 'Language',
    panelPort: 'Listen port',
    portHint: 'Restart required to take effect',
    basePath: 'Base path',
    basePathHint: 'e.g. /Mantou; leave blank for root. Restart required to take effect',
    basePathInvalid: 'Base path must not contain Chinese or special characters — use ASCII letters, digits and symbols (e.g. /Mantou)',
    panelConfig: 'Panel service',
    accountConfig: 'Login account',
    panelHttps: 'Enable panel HTTPS',
    panelCert: 'HTTPS certificate',
    panelCertHint: 'Select a loaded, valid certificate. Restart required to take effect',
    panelCertRequired: 'A valid certificate is required to enable panel HTTPS',
    panelDomain: 'Access domain',
    panelDomainPlaceholder: 'e.g. panel.example.com',
    panelDomainHint: 'Required and limited to one domain. The selected certificate must cover it, and HTTPS access is restricted to this domain.',
    panelDomainRequired: 'An access domain is required to enable panel HTTPS',
    themeMode: 'Theme mode',
    light: 'Light',
    dark: 'Dark',
    auto: 'System',
    colors: 'Theme colors',
    primary: 'Primary',
    accent: 'Accent',
    background: 'Background',
    bgType: 'Background type',
    bgColor: 'Solid color',
    bgGradient: 'Gradient',
    bgImage: 'Image',
    bgFollowTheme: 'Follow theme color (auto background)',
    bgFollowThemeHint: 'Background is auto-generated from the primary color and changes with it; uncheck to set a fixed solid color',
    bgValue: 'Background value',
    uploadBg: 'Upload image',
    deleteBg: 'Remove background',
    deleteBgConfirm: 'Remove the current background image? It will no longer be included in config backups.',
    deleteBgOk: 'Background image removed',
    blur: 'Background blur',
    overlay: 'Overlay opacity',
    card: 'Cards',
    cardOpacity: 'Card opacity',
    cardRadius: 'Corner radius',
    font: 'Typography',
    fontScale: 'Font scale',
    fontWeight: 'Font weight',
    preview: 'Live preview',
    previewText: 'This is sample text used to preview the current appearance.',
    resetAppearance: 'Reset to defaults',
    changePassword: 'Account & password',
    accountUsername: 'Login username',
    accountUsernameHint: 'Changing the username ends the current session — sign in again with the new username',
    oldPassword: 'Current password',
    newPassword: 'New password',
    newPasswordHint: 'Leave blank to keep the current password',
    confirmNew: 'Confirm new password',
    accountSaved: 'Account updated',
    logLevel: 'Log level',
    logConsole: 'Log to console',
    logMaxEntries: 'Max log entries',
    logMaxEntriesHint: 'The single master switch for how much log data this program keeps (default 1000, range 100–5000). It caps three stores independently: the program\'s own runtime logs, web-service access events, and the on-disk log file mantou.log. Applies immediately without a restart; shrinking keeps the newest in-memory entries. The disk file also has a fixed 5 MB size cap — whichever limit is hit first triggers rotation.',
    showOnHome: 'Show logs on overview',
    homeLimit: 'Overview log count',
    homeLimitHint: 'How many recent entries the overview "Program logs" panel renders (default 50, max 200, never more than the max entries above). This is display volume only: the panel redraws in full every 3 seconds, so its cap is deliberately far below the max entries.',
    levelDebug: 'Debug',
    levelInfo: 'Info',
    levelWarn: 'Warn',
    levelError: 'Error',
    logLevelAll: 'All levels',
    logLevelHint: 'Empty means record all levels',
    logFileInfo: 'Log file info',
    logPath: 'Current log path',
    logFiles: 'Log file count',
    logSize: 'Total log size',
    logSizeUnit: 'MB',
    logInfoLoading: 'Loading log info…',
    clearLogs: 'Clear all logs',
    clearLogsHint: 'Delete the current log file and its backups, then create a new empty log file (no restart needed)',
    clearLogsConfirm: 'Clear all log files? This deletes the current log and its backups, then immediately creates a new empty log file (no restart required).',
    clearLogsOk: 'Logs cleared',
    clearLogsFailed: 'Failed to clear logs',
    aboutTitle: 'About',
    aboutText: 'About text',
    aboutTextHint: 'Custom text shown on this page (optional)',
    aboutEmpty: 'No about text yet',
    updateConfig: 'Online update settings',
    manifestUrl: 'Version manifest URL',
    manifestUrlHint: 'URL returning JSON with a version field; leave blank to disable online checks',
    releaseUrl: 'Download page URL',
    releaseUrlHint: 'Page the “Update” button opens when a new version is found',
    selfUpdate: 'Upload update package',
    selfUpdateDesc: 'Upload a downloaded tar.gz package; the program replaces itself and restarts automatically (not supported on Windows)',
    selfUpdateBtn: 'Choose tar.gz and update',
    selfUpdateConfirm: 'This will replace the running program with the uploaded package and restart. Continue?',
    selfUpdateUploading: 'Uploading and applying the update package…',
    selfUpdateOk: 'Update applied — the service is restarting, please refresh shortly',
    selfUpdateBadFile: 'Please choose a .tar.gz update package',
    exportConfig: 'Export configuration',
    exportBtn: 'Download encrypted backup',
    exportDesc: 'Download all current configuration as an encrypted JSON file for backup or migration',
    exportWarn: 'The export contains sensitive data such as login info, session key and provider credentials — keep it safe',
    exportEncrypt: 'Verify login password',
    exportPwdHint: 'The exported backup is encrypted with your current login password — enter it here',
    exportEncryptedHint: 'The backup is encrypted with your login account + password. Remember the password — it cannot be recovered if lost.',
    importConfig: 'Import configuration',
    importDesc: 'Restore configuration from a previously exported JSON file',
    importWarn: 'Importing overwrites all current configuration; some changes need a restart',
    chooseFile: 'Choose config file',
    importConfirm: 'Importing will overwrite all current configuration. Continue?',
    importConfirmPartial:
      'Only "{modules}" will be overwritten from the backup; the other modules keep their current data. Continue?',
    // Selective import: pick which modules the backup actually overwrites. All by default.
    importScope: 'Import scope',
    importScopePanel: 'Panel & settings',
    importScopeAll: 'Select all',
    importScopeNone: 'Clear all',
    importScopeLocked: 'referenced by a selected module, imported together',
    importScopeHint:
      'Everything is imported by default. Unchecked modules keep their current data — they are not cleared.',
    importScopeEmpty: 'Select at least one module to import',
    // Master key notice: copying the data dir by hand must include master.key.
    masterKey: 'Config master key',
    masterKeyDesc:
      'master.key in the data directory is the only key that unlocks the credential fields inside config.json (DNS provider credentials, ACME account private keys, the session signing key and the 2FA secret). Those fields are stored encrypted, because file permissions alone cannot stop the whole data directory from being carried away — backups, mounted volumes, snapshots, an accidental commit.',
    masterKeyWarn:
      'If you back up by copying the data directory, copy master.key along with it; without it the program fails loudly at startup instead of silently losing the credentials. A backup made with "Export configuration" on this page has no such requirement: its credentials are encrypted as a whole with your account name plus login password, so importing it on a new machine works immediately without master.key.',
    masterKeyEnvHint:
      'The key may also be supplied through the MANTOU_MASTER_KEY environment variable (32 bytes as hex or base64). No master.key file exists on disk then, which suits handing the key to a container secret or a systemd credential.',
    // Storage: files in the data directory nothing points at any more
    storageTitle: 'Disk usage',
    storageList: 'Unreferenced files',
    storageDesc:
      'Nothing in your configuration points at these files any more: background images you replaced, certificate and key files left behind after a certificate was deleted, staging directories left by an import that was interrupted. They do not affect anything at runtime, they just keep taking up disk. Tick the ones to remove, then click Clean up selected.',
    storageRescan: 'Rescan',
    storageCleanup: 'Clean up selected',
    storageTotal: '{count} items, {size} in total',
    storagePicked: '{count} selected, {size} in total',
    storageEmpty: 'No unreferenced files',
    storageTruncated: 'Many files here; only the first {limit} are listed. Scan again after cleaning up.',
    storageNoteFresh: 'Just uploaded',
    storageKind: {
      upload: 'Upload',
      cert: 'Certificate file',
      restore: 'Import staging',
      temp: 'Temporary file',
    },
    storageLoadFailed: 'Could not read disk usage',
    storageCleanupConfirm: 'This removes the {count} selected items ({size} in total) and cannot be undone. Continue?',
    storageCleanupOk: 'Removed {count} items, freed {size}',
    storageCleanupSkipped: '{count} items are no longer in the list and were skipped',
    storageCleanupFailed: 'Could not remove {count} items',
    // Import step 1: verify the local administrator. Kept in its own dialog, separate from
    // the backup passphrase, so only one password field is ever on screen at a time.
    importAuthTitle: 'Verify identity',
    importAuthDesc: 'Importing overwrites the configuration on this machine, which may include the administrator account. Confirm that you are this panel\'s current administrator.',
    importAuthAccount: 'Current account',
    importAuthPassword: 'Current password',
    importAuthPwdHint: 'The password you sign in to this panel with, not the backup file\'s passphrase',
    importAuthNext: 'Next',
    importDecrypt: 'Decrypt backup',
    importAccount: 'Account',
    importPassword: 'Password',
    importPwdHint: 'Enter the account name and password used when the backup was created',
    importDecryptFail: 'Decryption failed: account name or password is incorrect',
    sessionHours: 'Token lifetime (hours)',
    sessionHoursHint: 'Applies to tokens issued by future logins only',
    sessionIdleMinutes: 'Idle timeout (minutes)',
    sessionIdleMinutesHint: 'The session expires after this long without a single request; 0 disables it',
    loginMaxFails: 'Max failed login attempts',
    loginMaxFailsHint: 'The account is locked after this many failures; 0 disables the limit',
    loginLockMinutes: 'Lock duration (minutes)',
    loginLockMinutesHint: 'How long the account stays locked before it auto-unlocks',
    // Outbound private-network guard
    outboundGuard: 'Outbound request guard',
    blockPrivateNetwork: 'Block private / reserved addresses',
    blockPrivateNetworkHint: 'Off by default; keep it off if you fetch IPs or call back from an intranet address',
    blockPrivateNetworkDesc: 'Covers the four kinds of outbound request whose target you supply: DDNS "custom URL" IP detection, scheduled-task HTTP actions, message-target delivery, and fetching the online-update manifest. When enabled, a request is refused if the target resolves to a private or reserved address (e.g. 127.0.0.1, the 10 / 172.16 / 192.168 private ranges, 169.254 link-local, or 100.64/10 carrier-grade NAT). This stops someone from steering the server into probing internal services by supplying an internal address, and every block is logged for auditing. Keep it off if you legitimately need to reach an IP-detection service, a webhook, or a self-hosted update manifest on your LAN.',
  },
  about: {
    title: 'About Mantou',
    subtitle: 'Version, updates and program info',
    uploadTitle: 'Upload update package',
    uploadDesc: 'Upload a downloaded tar.gz package; the program replaces itself and restarts automatically (not supported on Windows). Only upload packages from a source you trust; if a signing key is configured under Settings → Online update, the package must include a matching .sig signature and pass verification before it is applied.',
    signKeyMissing: 'No self-update signature public key is configured and unsigned packages are not allowed, so update packages are not accepted right now. Configure an Ed25519 public key under Settings → Online update, or turn that option on.',
    uploadBtn: 'Choose tar.gz and update',
    uploadBadFile: 'Please choose a .tar.gz update package',
    uploadConfirm: 'This will replace the running program with the uploaded package and restart. Continue?',
    uploading: 'Uploading and applying the update package…',
    uploadOk: 'Update applied — the service is restarting, please refresh shortly',
    restartCountdown: 'Update applied — the service is restarting and the page will auto-refresh in {n} seconds.',
    site: 'Website',
    currentVersion: 'Current version',
    currentArch: 'Current architecture',
    versionInfo: 'Version info',
    latestVersion: 'Latest version',
    compileTime: 'Build time',
    download: 'Download',
    description: 'About',
    checking: 'Checking…',
    networkUnavailable: 'Current network unavailable',
    rateLimited: 'GitHub API rate limited, please try later',
    // Run instructions (the collapsible block inside "About"): unpack + start commands per platform.
    // Platform names (Linux amd64 (x86_64) / macOS arm64 (Apple Silicon) / Docker …) are fixed
    // strings kept in the component, not in the language pack.
    runTitle: 'How to run',
    runHint: 'Download the archive for your platform, unpack it, and start it with the commands below. --data sets the data directory; without it, a data folder under the current working directory is used.',
    runCurrent: 'current',
    runCopy: 'Copy',
    runCopied: 'Command copied',
    runCopyFail: 'Copy failed, select the command manually',
    runNoteMac: 'Do not skip the third line: executables downloaded through a browser carry a quarantine flag and are blocked from running directly.',
    runNoteWin: 'Unpacking gives you mantou.exe; double-click it and it creates a data folder next to itself. The command above is only needed to point it at a different data directory. The firewall prompts on first run, choose allow, otherwise other devices on the LAN cannot reach the panel.',
    runNoteDocker: 'The image is multi-arch, so pull picks the variant matching your machine, amd64 and arm64 use the same command. Add --platform linux/arm64 only when you want the other architecture.',
    // Kept here in the language pack so it ships with the UI. It used to be read at
    // runtime from a README.md next to the executable, but the update archive holds
    // only the executable and its signature — the README is not shipped, so after a
    // hot update this section came up empty.
    programDesc:
      'Mantou is a small self-hosted panel the developer built for their own use. It gathers a handful of routine chores into one interface: dynamic DNS, websites and reverse proxying, port forwarding, wake-on-LAN, scheduled tasks, automatic certificate renewal, and message forwarding. It is a single executable with the interface bundled inside — no database or extra services to install.\n\nRough memory footprint: 15–25 MB when idle, 30–80 MB in everyday use, and up to 100–200 MB when forwarding heavy traffic. 128 MB is a comfortable allowance, 256 MB for sustained heavy load, and going below 64 MB is not recommended. On disk the program itself is about 13 MB, and the data it produces normally stays under 50 MB.\n\nThe developer wrote this to fit their own needs, so the feature set is scoped to what is enough in practice rather than to covering every scenario.',
    unknown: 'Unknown',
  },
  mroute: {
    title: 'Message Routing',
    subtitle:
      'Receive webhooks from third-party systems, evaluate conditions, render a message, and deliver it to DingTalk / WeCom / a custom HTTP endpoint.',
    running: 'Running',
    abnormal: 'Error',
    stopped: 'Disabled',
    moduleSettings: 'Module settings',
    moduleIntro:
      "This module's own inbound listener: port, domain and HTTPS. Every receiver URL hangs off this one listener.",
    disabledHint:
      'The module is not enabled yet. Open the "Module settings" tab to start the listener — receiver URLs only accept pushes once it is running.',
    mReceived: 'Received',
    mRejected: 'Rejected',
    mSent: 'Sent',
    mFailed: 'Send failed',
    mPending: 'Retry queue',
    mDropped: 'Dropped',
    tabReceivers: 'Receivers',
    tabTargets: 'Notification targets',
    tabTemplates: 'Message templates',
    tabRules: 'Send rules',
    tabHistory: 'History',
    recvIntro: 'One receiver per source system: its own URL, its own auth, its own rules.',
    targetIntro: 'Where messages end up. One target can be shared by rules across many receivers.',
    rulesIntro:
      'A rule is "which inbound URL → under what condition → with which template → to whom". Rules are compared by priority, lowest first, and stop at the first match.',
    rulesNoRecv: 'No receivers yet: create an inbound URL on the Receivers tab first — a rule needs one.',
    rulesEmpty: 'No send rules yet. Click Add above: a message is only rendered and delivered once a rule matches.',
    tmplIntro: 'What the message looks like. Click a field on the right to insert it — no code needed.',
    note: 'Note',
    copy: 'Copy',
    copied: 'Copied',
    copyFail: 'Automatic copy is not allowed here; please select and copy manually',
    fields: 'Fields',
    sample: 'Sample payload',
    samplePlaceholder: 'Paste a real JSON payload from the third-party system here',
    sampleHint:
      'Paste a real payload and every field is listed below — click one to use it. The sample is kept in this browser only and destroyed after 3 hours.',
    sampleGone: 'The sample payload passed its 3-hour lifetime and was destroyed. Run a test run again if you still need it.',
    sampleBad: 'This is not valid JSON, so no field tree can be built.',
    sampleBadKV:
      'The current separators yield no fields at all: check the source message type, or set the separators manually on the left.',
    treeEmptyParse: 'Paste a sample payload on the "Parsing" tab and the available fields will appear here.',
    tree: {
      empty: 'Paste a sample payload and every field will be listed here.',
      arrayTag: 'array',
    },
    op: {
      countGt: 'count greater than',
      countGte: 'count at least',
      countLt: 'count less than',
      countLte: 'count at most',
      countEq: 'count equals',
      eq: 'equals',
      ne: 'not equal',
      contains: 'contains',
      notContains: 'does not contain',
      prefix: 'starts with',
      suffix: 'ends with',
      in: 'is one of',
      regex: 'matches regex',
      gt: 'greater than',
      gte: 'greater or equal',
      lt: 'less than',
      lte: 'less or equal',
      exists: 'field exists',
      empty: 'is empty',
    },
    cond: {
      groupValue: 'Compare the value',
      groupCount: 'Compare how many',
      countPlaceholder: 'Count, e.g. 1',
      countHint:
        'Count operators compare how many values the path resolves to. With 2 values under "creator", "count greater than 1" matches.',
      pathRequired: 'A path is required, otherwise the condition never matches.',
      pathPlaceholder: 'Field path, e.g. body.billType',
      inPlaceholder: 'Comma-separated values',
      valuePlaceholder: 'Value',
      noValueNeeded: 'This operator takes no value',
      not: 'Negate',
      notHint: 'When checked, the condition means "does NOT match"',
      add: 'Add condition',
      emptyMeansAlways: 'No conditions = always matches (useful as a catch-all rule).',
      limit: 'Up to {n} conditions',
    },
    event: {
      received: 'Received',
      rejected: 'Rejected',
      dropped: 'Dropped',
      error: 'Error',
      sent: 'Sent',
      failed: 'Failed',
      retrying: 'Retrying',
    },
    hist: {
      allReceivers: 'All receivers',
      allEvents: 'All events',
      time: 'Time',
      event: 'Event',
      rule: 'Matched rule',
      target: 'Target',
      detail: 'Detail / source',
      cost: 'Duration',
      memoryOnly:
        'History lives in memory only and is cleared on restart; failures and rejections are also written to the system log. Memory use is capped at 1 MB (about 2000 entries, fewer when failure reasons are long); past that the oldest entries are dropped, and dropped entries remain in the log file. Rejected and discarded entries also keep a copy of what the sender sent; how much memory that may use is set under Module settings → Retained payloads, and it is likewise cleared on restart.',
      viewSource: 'View payload',
      sourceTitle: 'What the sender sent',
      sourceGone:
        'This payload is no longer in memory — it may have been pushed out by newer entries, or cleared just now. Payloads are kept in memory only, so check soon after the problem happens.',
      srcRequest: 'Request',
      srcQuery: 'URL parameters',
      srcHeaders: 'Headers',
      srcBody: 'Body',
      srcTruncated: 'truncated',
      srcBodyUnread:
        'The body was not read this time — the auth, allow-list and rate checks run before the body is read, so nothing the sender sent was kept. This does not mean the sender sent an empty body.',
      srcBodyEmpty: 'The sender sent an empty body',
      sourceUsage:
        'Keeping {n} of {max} payloads, using {used} of {budget}; each body is kept up to {body} and truncated beyond that.',
      clearSources: 'Clear payloads',
      clearSourcesConfirm:
        'Clear every retained payload? These live in memory only, so they cannot be recovered. The size limit stays as it is, and later messages are retained as usual.',
      clearSourcesOk: 'Cleared {n} payloads',
      total: '{n} entries',
      pageSize: 'Rows per page',
    },
    srv: {
      enabled: 'Enable module',
      port: 'Listen port',
      portHint: 'Separate from the panel port; it must not clash with the panel or any web service.',
      https: 'Enable HTTPS',
      httpsOnly:
        'With HTTPS on, this port accepts HTTPS only and rejects plain HTTP — update the URL configured in the third-party system to https as well.',
      cert: 'Certificate',
      certRequired: 'A certificate is required when HTTPS is enabled',
      certHint:
        'The certificate must be enabled and unexpired. Disabling a certificate referenced here warns that this module is using it.',
      domain: 'Access domain',
      domainHint:
        'The domain third-party systems will call. Required when HTTPS is on (and it must be covered by the selected certificate), and required on ports 80 / 443. Receiver URLs are built from it. Enter one exact hostname — no port, and no wildcards such as *.example.com.',
      domainCol: 'Domain',
      domainMissing: 'Not set (required on 80 / 443)',
      addrCol: 'Listen address',
      receivedCol: 'Received',
      notePlaceholder: 'What this inbound listener is for — for your eyes only',
      sourceRetain: 'Retained payloads (MB)',
      sourceRetainHint:
        'For rejected and discarded messages, a copy of what the sender sent is kept in memory and shown when you click Detail / source on that history row. Retained payloads are never written to a file and are cleared on restart. Set 0 to keep none; at most {max} MB. This is what you look at when a message never arrived; turn it off if you are not investigating.',
      publicPortHint:
        'Ports 80 / 443 are the defaults for browsers and third-party systems, and the panel, web services and message routing may all want them: an access domain is required so requests can be routed to this module by host name. A domain cannot be reused on the same port, nor can it be the panel domain. If a web service already holds the port, this module rides on that listener and receives by domain, so the HTTPS switches on both sides must match.',
      listening: 'Listening on {addr}',
      notCreated: 'There is no inbound listener on this machine yet: create the module first so receivers get a reachable address.',
      emptyHint:
        'No module yet. Click "Add" above to set the listen port and access domain — once it exists, receivers are filled in with that domain and a random path.',
      deleteConfirm:
        'Deleting stops the inbound listener at once and clears the port, access domain and certificate settings, so nothing pushed by third-party systems will arrive. Receivers, message templates, notify targets and send rules are all kept — recreate the module to resume. Delete it?',
      deleted: 'Module deleted, inbound listener stopped',
    },
    recv: {
      needModule:
        'The message-routing module has not been created, so receivers cannot be enabled: create it under "Module settings" first (listen port and access domain). Receivers can be configured meanwhile and enabled afterwards.',
      sourceType: 'Source message type',
      sourceAuto: 'Auto-detect (recommended)',
      sourceKV: 'Key-value text',
      sourceTxt: 'Plain text',
      sourceTypeHint:
        'Auto-detect is the default and decides per message: valid JSON is parsed as JSON, key-value text such as name=abc&type=1 is split into fields, anything else goes into body as one string. One source may mix both formats and still match the same rule and template — the field names are usually the same in both. Change this only to force one specific format.',
      kvSeps: 'Separators',
      kvPairSep: 'Between fields',
      kvKVSep: 'Name and value',
      kvAuto: 'Auto-detect',
      kvSepsHint:
        'Leave both blank to auto-detect; fill them in only when the values themselves contain these symbols and the split comes out wrong. Use \\n for a newline, \\t for a tab.',
      kvSniffed: 'Currently splitting fields on "{pair}" and names from values on "{kv}": {n} fields.',
      detected: 'This sample is parsed as "{type}".',
      detectMismatch:
        'This sample looks like "{type}", but the type is fixed to "{current}", so paths will most likely resolve to nothing. Switch the source message type to auto-detect to accept both formats.',
      parseTestHint:
        'Click "Start live capture", then trigger a push in the source system: the raw message lands in the sample below and the field tree lists every field. Nothing is forwarded while capturing.',
      testRunNeedSave: 'Save this receiver first — live capture needs its inbound address.',
      mapNamePlaceholder: 'Alias, e.g. nickname',
      mapPathPlaceholder: 'Path, e.g. body.sorname',
      mapExample:
        'Example: alias "nickname" with path "body.sorname" — then just write nickname in conditions and templates.',
      mapPathRequired: 'A path is required, otherwise the alias never resolves.',
      mapArrayHint:
        'A path containing [*] only ever resolves to the first entry. To list them all in a template, alias the array itself (e.g. alias "list" with path "items") and use "List items one per line" there.',
      arrayFound: '{n} array field(s) in this sample have no alias yet',
      arrayAddAlias: 'Add aliases',
      arrayHint:
        'An array is the one kind of field a template can only render through a loop. Once it has an alias, "List items one per line" in the template editor builds that loop under your own name for it — no need to remember the full path.',
      arrayAllAliased:
        'Every array field is aliased: {list}. Use "List items one per line" in the template editor to lay them out row by row.',
      add: 'New receiver',
      edit: 'Edit receiver',
      namePlaceholder: 'e.g. Third-party system push',
      notePlaceholder: 'Who this source is and what it pushes',
      tabBasic: 'Basics',
      tabSecurity: 'Security',
      tabParse: 'Parsing',
      path: 'Inbound path',
      pathPlaceholder: 'Leave blank to generate a random path on save',
      pathHint:
        'The random path is the main protection for this endpoint — keep it private. A custom path is allowed, but then make sure auth is in place.',
      pathWeak:
        'Authentication is off and this path is shorter than {n} characters. With that combination, anyone who guesses the URL can push data here. Use “Regenerate” for a random path, or pick a check on the “Security” tab. Saving still works.',
      regen: 'Regenerate',
      fullUrl: 'Full inbound URL',
      fullUrlHint:
        'Put this URL into the third-party webhook configuration. It changes with the port / domain in module settings.',
      authType: 'Authentication',
      authNone: 'None',
      authToken: 'Fixed token',
      authHeader: 'Custom header',
      authHint: {
        none: 'No credential check — only the random path protects it. Suitable for trusted intranet sources only.',
        token: 'Requires the given header to be present and equal to the token below.',
        header:
          'Requires the given header to be present and equal to the token below (same check as fixed token, only the header name differs so it can match the other side’s docs).',
      },
      headerName: 'Header name',
      token: 'Token',
      rateLimit: 'Rate limit (per minute)',
      rateLimitHint: '0 means unlimited. Requests beyond the limit are rejected and recorded in history.',
      maxBody: 'Body limit (KB)',
      maxBodyHint: 'Larger bodies are rejected, so a single push cannot exhaust memory.',
      ipFilter: 'IP list',
      ipFilterOn: 'Enable IP list',
      ipMode: 'List mode',
      ipDeny: 'Deny list',
      ipAllow: 'Allow list',
      allowList: 'Allowed IPs / CIDRs',
      denyList: 'Denied IPs / CIDRs',
      ipPlaceholder: 'e.g. 203.0.113.5 or 10.0.0.0/8, press Enter to add',
      ipHint:
        'Matched against the real connection source, not X-Forwarded-For (which can be forged). If a reverse proxy sits in front, list the proxy address here.',
      keywordFilter: 'Keyword gate',
      keywordOn: 'Enable keyword gate',
      keywordOnHint:
        'Same idea as the custom keywords of a DingTalk / WeCom robot, in reverse: the message you receive must contain one of the agreed words to pass, otherwise it is rejected. ' +
        'A path and a token only prove who is pushing, not what is being pushed — if the token leaks, or the other side points another workflow at this URL by mistake, this is the only layer that stops it.',
      keywordMode: 'Match mode',
      keywordAny: 'Any word is enough',
      keywordAll: 'All words required',
      keywords: 'Keywords',
      keywordPlaceholder: 'Type a keyword, press Enter to add',
      keywordHint:
        'Searched in the raw content, case-insensitively, whether the source sends JSON, plain text or a txt file — appearing anywhere in the text counts (query-string values are included too). ' +
        'Rejected requests only get a generic refusal; which word was missing goes to the execution history, never back to the sender.',
      keywordEmpty:
        'Leaving the list empty while the switch is on would reject every message this receiver gets; saving will be refused.',
      rootPath: 'Payload root path',
      rootPathHint:
        'Where the business data sits. "body" means paths in conditions and templates start at the request body root; blank uses the whole envelope (body / headers / query / method …).',
      mappings: 'Field aliases',
      urlCol: 'Inbound URL',
      mapDefault: 'Default',
      mapNote: 'Note (optional)',
      addMapping: 'Add alias',
      mappingsHint:
        'Give a deep path a short name and use the alias in conditions and templates. The default is used when the path resolves to nothing.',
      mapClash: 'An alias must not collide with a built-in field ({list}), or the envelope will shadow it.',
      // conditions / template stay under recv: the "referenced template or target is missing"
      // line in the receiver list and the Send rules column header mean the same thing,
      // so they must not have two separate translations.
      conditions: 'Conditions',
      template: 'Message template',
      fallbackSection: 'Fallback',
      defaultTargets: 'Fallback notify targets',
      defaultTargetsPlaceholder: 'Optional',
      defaultTargetsHint: 'Only used when a rule picks no target of its own. Normally you give each rule its own targets and leave this empty.',
      rulesCol: 'Rules',
      lastRecv: 'Last received',
      countCol: '{n} received',
      // Two side-by-side counts, not a subset: "{n} total ({r} rejected)" reads as
      // "4 arrived, all 4 blocked" when in fact 4 got through and 4 more were blocked.
      countColRejected: '{n} received · {r} rejected',
      badRefs: 'Referenced template or target is missing / disabled: {list}',
    },
    rule: {
      add: 'New send rule',
      edit: 'Edit send rule',
      name: 'Rule name',
      namePlaceholder: 'e.g. Forward important messages to the notify group',
      priority: 'Priority',
      priorityHint:
        'Compared lowest first, stopping at the first match. Left at 0, the rule is placed after the existing rules of that receiver.',
      recvOff: 'Receiver disabled',
      receiver: 'Which receiver',
      receiverHint:
        'This rule only looks at messages arriving at this receiver. Picked the wrong one? Change it here — the rule moves over with its conditions.',
      receiverRequired: 'Pick the receiver this rule belongs to first.',
      stepFrom: 'Which inbound URL',
      stepWhen: 'When does it match',
      stepRender: 'How it looks',
      stepSend: 'Where it goes',
      match: 'Condition logic',
      matchAll: 'Match all',
      matchAny: 'Match any',
      conditions: 'Conditions',
      conditionsHint:
        'With no conditions at all, every message this receiver takes in matches — useful as a catch-all rule.',
      condAny: 'Match all',
      template: 'Message template',
      templateHint: 'Which template renders this rule. Every rule can use a different one.',
      targets: 'Notify targets',
      targetsPlaceholder: 'Pick one or more targets',
      targetsHint: 'Where this rule sends once it matches; pick as many as you like.',
      targetsFallback: 'Receiver fallback',
      usingFallback: 'No targets picked, so the receiver fallback is used: {names}',
      noTargetWarn:
        'This rule has no targets and its receiver has no fallback targets either: it would match and still send nothing, so saving will be rejected. Pick targets here, or set fallback targets on the receiver Basics tab.',
      continue: 'Keep going through the later rules',
      continueHint:
        'Off by default: processing ends once this rule has run. When on, the rules after this one are processed too, which may send more messages.',
      fieldsHint:
        'These fields come from the shared sample payload, parsed with the selected receiver settings. Click one to copy its path into a condition above.',
      duplicate: 'Duplicate',
      copyName: '{name} copy',
      copyHint:
        'Start a new rule from this one (conditions, template and targets come along) and change what differs. To send the same messages to different people by finer conditions, use "Multiple branches" inside the rule instead.',

      // ---- Branches ----
      multi: 'Multiple branches',
      multiHint:
        'Send the same messages to different people, with different templates, by finer conditions. Off means one template to one set of targets.',
      twoLayer:
        'The conditions in section 2 above are the "overall" conditions: a message passes those first, and only then reaches the branches below. A branch only adds the conditions that decide which exit it takes — no need to repeat the overall ones.',
      stepBranch: 'Which branch to send',
      conditionsHintMulti:
        'These are the overall conditions (coarse filter): if they fail, the whole rule is skipped and no branch is even evaluated. With none at all, every message reaches the branches.',
      branchAdd: 'Add branch',
      branchCount: '{n} branches',
      firstOnlyTag: 'First match only',
      branchEmpty: 'At least one branch is needed, otherwise the rule has no exit.',
      branchLimit: 'At most {n} branches.',
      branchNameN: 'Branch {n}',
      branchNamePlaceholder: 'e.g. Urgent',
      branchCond: 'Extra conditions (applied on top of the overall ones)',
      branchCatchAll:
        'With no conditions it matches whenever the overall conditions pass — the catch-all exit for this rule.',
      branchTemplatePlaceholder: 'Which template this branch uses',
      branchUp: 'Move up',
      branchDown: 'Move down',
      branchDup: 'Duplicate this branch',
      branchNameRequired:
        'Every branch needs a name: run history tells exits apart by "rule name / branch name".',
      branchNameDup: 'Duplicate branch names: {names}. Same-named branches are indistinguishable in run history.',
      branchTemplateRequired: 'Every branch needs a message template.',
      branchNoCatchAll:
        'Every branch carries conditions, so this rule has no catch-all exit: when the overall conditions pass but no branch condition does, nothing is sent (run history records "matched the rule, but no output branch condition held"). Add a branch with no conditions at the end to catch the rest.',
      firstBranchOnly: 'Branch evaluation',
      firstBranchOnlyAll: 'Send them all',
      firstBranchOnlyFirst: 'Stop at first match',
      firstBranchOnlyOn:
        'Evaluated top to bottom, stopping at the first branch whose conditions hold; the branches below it are not evaluated.',
      firstBranchOnlyOff:
        'Every branch whose conditions hold is sent (use this when the branch conditions are unrelated to each other).',
      firstBranchOnlySame:
        'This choice only decides what happens when one message satisfies several branch conditions at once. If the branch conditions are mutually exclusive — no single message can satisfy two of them — both choices take the one branch that matched and send exactly the same messages. It is also not part of validation: the rule saves either way.',
      multiOffTitle: 'Turn off multiple branches',
      multiOffConfirm:
        'Only the first branch "{name}" keeps its template and targets; the other {n} branches and their conditions will be deleted. Turn it off?',
    },
    target: {
      add: 'New target',
      edit: 'Edit target',
      namePlaceholder: 'e.g. Company DingTalk group',
      notePlaceholder: 'Who reads this group',
      type: {
        label: 'Type',
        dingtalk: 'DingTalk bot',
        wecom: 'WeCom bot',
        http: 'Custom HTTP',
      },
      url: 'Webhook URL',
      urlHint:
        'The full bot webhook URL including access_token / key. It is not shown again in the list after saving.',
      secret: 'Signing secret',
      secretHint: 'Fill this when the DingTalk bot uses "signed" security; leave blank for IP range or keyword mode.',
      atMobiles: 'Mention by phone',
      atMobilesPlaceholder: 'Phone number, press Enter to add',
      atMobilesHint: 'Mentioned people must be in the group. Leave empty to mention nobody.',
      atAll: 'Mention everyone',
      wecomAtNote: 'WeCom supports mentions in text messages only: with a Markdown template, mention settings are ignored.',
      method: 'Method',
      contentType: 'Content-Type',
      headers: 'Custom headers',
      headerName: 'Name',
      headerValue: 'Value',
      addHeader: 'Add header',
      headersHint: 'Typically the auth header the other side requires, e.g. Authorization. Up to {n}.',
      bodyTemplate: 'Request body template',
      bodyTemplateHint:
        'Available: {expr} (the rendered body, quoted and escaped so it can go straight into JSON), plus message / title / format / event. Leave blank to post the plain-text body.',
      timeout: 'Timeout (s)',
      retry: 'Retries',
      retryHint:
        'Failed sends are retried with backoff; if the retries are exhausted it is written to history and the system log. The retry queue is in memory only and is cleared on restart; it holds at most 1024 messages and 1 MB total (fewer when messages are large). Anything that does not fit is recorded as "queue memory full" rather than silently dropped.',
      test: 'Test send',
      testOk: 'Sent successfully ({ms} ms)',
      testFail: 'Send failed',
      testTitle: 'Test send · {name}',
      testSend: 'Send',
      testDefault: 'This is a test message from Mantou. Receiving it means this notify target works.',
      testFormatHint:
        'Format only affects DingTalk / WeCom; for custom HTTP the payload is whatever the target\'s body template builds.',
      testTitlePlaceholder: 'Defaults to "Mantou test message"',
      testBodyPlaceholder: 'Type what should be sent — it goes out as-is',
      testNoVars:
        'A test send has no receiver behind it, so there are no fields to pull: whatever you type is sent verbatim and template syntax is not rendered.',
      lastSent: 'Last sent',
      counts: '{sent} ok / {fail} failed',
    },
    tmpl: {
      noCondHint:
        'Templates carry no "who gets this" conditions: that is decided by the receiver rules. Here you only fill in fields or aliases.',
      add: 'New message template',
      edit: 'Edit message template',
      duplicate: 'Duplicate',
      copyName: '{name} copy',
      namePlaceholder: 'e.g. Generic template',
      notePlaceholder: 'Where this template is used',
      format: 'Format',
      text: 'Plain text',
      title: 'Title',
      titlePlaceholder: 'Title shown in the chat list for Markdown messages',
      titleHint:
        'DingTalk / WeCom Markdown messages require a title — it is the line shown in the message list. Whether it also appears in the body, and how, is set by "Title style" below.',
      titleStyle: 'Title style',
      titleStyleHint:
        "DingTalk shows the title only in the chat list, never inside the message bubble, and WeCom Markdown has no title field at all — so the title has to go into the body to be visible. Pick how it looks here. Nothing is inserted if the body's first line already is the same title.",
      style: {
        h1: 'Heading 1 (largest)',
        h2: 'Heading 2',
        h3: 'Heading 3 (recommended)',
        bold: 'Bold line',
        none: 'Not in body (chat-list preview only)',
      },
      wecomMdNoAt: 'WeCom does not support mentions in Markdown; use plain text if you need to mention someone.',
      body: 'Body',
      bodyPlaceholder:
        'Click a field on the right to insert it, or use "Build reference template" below to get a working body right away.',
      bodyCol: 'Body excerpt',
      updated: 'Updated',
      build: 'Quick start',
      reference: 'Build reference template',
      referenceHint:
        "Builds a whole body from this receiver's aliases and sample fields: every name and path really exists, so you only reword the labels and delete the lines you don't want. No filler text is added.",
      refNoFields:
        'No fields available yet: pick a receiver that has aliases above, or paste a sample under Receivers → Parsing, or capture a real message with a test run.',
      refConfirm: 'The body already has content and will be replaced entirely. Continue?',
      refDone: 'Reference template built from your own fields',
      defaultWord: 'n/a',
      previewTitle: 'Preview',
      previewLive: 'Live',
      previewRendering: 'Rendering…',
      previewSampleTitle: 'Sample payload',
      previewFromCapture: 'Capture, unchanged',
      previewSample: 'Paste something the third party sends, or use "Take latest capture" below',
      previewHint:
        'Rendering goes through the same code as real delivery, so this is exactly what the notification will look like. Previewing never sends anything.',
      previewNoSample:
        'No sample payload yet: expand "Sample payload" below and paste something the third party sends, or run a test on the receiver to capture a real message — the preview renders as soon as there is data.',
      previewNoBody:
        'The body is still empty: use "Build reference template" below, or click a field on the right to insert its value — this updates as you type.',
      previewEmpty:
        'This template rendered nothing for this sample. Check that the paths in the body match the fields on the right.',
      previewNoRecv: 'No receiver is being used, so aliases resolve to nothing. Pick a receiver on the right first.',
      previewTruncated: 'Output too long, truncated',
      useCapture: 'Take latest capture',
      snippets: 'Common snippets',
      snipDefault: 'Default value',
      snipEach: 'List items one per line',
      snipEachNone:
        '"List items one per line" only applies to arrays, and the current sample has no group of records — hence the greyed-out button. Paste a sample that contains a group of records, or capture a real message with a test run.',
      snipEachAliased:
        '"List items one per line" inserts a loop over the alias "{name}": the array path and element field names are already filled in, so just reword the labels.',
      snipEachRaw:
        '"List items one per line" will loop over the raw sample path "{path}". That works, but aliasing it under "Receiver → Parsing" first keeps the body far shorter and makes the section recognisable.',
      snipEachNoList:
        'Neither the aliases nor the sample contain a group of records (an array), so there is nothing to loop over. Capture a real message or paste a sample first.',
      arrNoAliasTitle: 'This group of records has no alias yet',
      arrNoAliasTip:
        '{list} in the sample is a group of records (an array) with no alias yet. Give it a name under "Receiver → Parsing → Field aliases" and the loop inserted below gets much shorter — and a path change at the source only has to be fixed in that one place.',
      arrNoAliasMsg:
        '{list} in the sample is a group of records (an array) and has no alias under "Parsing → Field aliases" of receiver "{recv}". With an alias, the loop in your body reads as the name you chose, and a path change at the source only has to be fixed on the receiver. Inserting it with the raw path works too.',
      // Used when no receiver is selected (or it has no name), so the text never says receiver "".
      arrNoAliasMsgAny:
        '{list} in the sample is a group of records (an array) and has no alias under "Parsing → Field aliases" on the receiver. With an alias, the loop in your body reads as the name you chose, and a path change at the source only has to be fixed on the receiver. Inserting it with the raw path works too.',
      arrNoAliasRaw: 'Insert with raw path',
      arrNoAliasGo: 'I will add an alias',
      snipBreak: 'Line break',
      newlineHint:
        'Line breaks differ per channel: DingTalk markdown ignores a single newline and needs a blank line, while WeCom markdown and plain text break on one. "Line break" inserts whatever the current format needs.',
      fields: 'Field aliases',
      recvPicker: 'Pick a receiver',
      recvPickerHint:
        'These are the aliases from that receiver\'s Parsing → Field mapping: click an alias to insert its value, or "List" for a loop.',
      noReceiver:
        'No receivers yet. Create one on the Receivers tab and name the fields you need under Parsing → Field mapping.',
      noMappings:
        'This receiver has no field mappings yet. You can build the template straight from "Raw fields" below, or add aliases under Receivers → Parsing → Field mapping.',
      payloadFields: 'Raw fields (no alias needed)',
      payloadHint:
        'Clicking a field inserts its full path (starting at body, independent of the receiver\'s root path); arrays are inserted as a loop skeleton.',
      payloadEmpty:
        'No sample payload yet. Paste something the third party sends under Receivers → Parsing, or capture a real message with a test run, and its fields show up here.',
      asList: 'List',
      builtinFields: 'Built-in fields',
      builtinHint: 'These are available in any template, regardless of the payload.',
      funcs: 'Functions',
      funcsHint: 'Click to insert a skeleton, e.g. fixed for decimals or formatTime for timestamps.',
    },
    dry: {
      start: 'Start live capture',
      stop: 'Stop live capture',
      badge: 'Capturing',
      stopped: 'Live capture stopped — messages are forwarded again',
      sampleBtn: 'Dry run',
      startHint: 'While on, real messages to this receiver are only shown here and never forwarded; stopping restores delivery at once. Disabled receivers can be captured too, so you can get it right before going live.',
      liveNote: 'Capturing: messages received right now go nowhere. Stop to resume delivery.',
      remain: 'auto-stops in {time}',
      got: '{n} received',
      waiting: 'Waiting for messages... ...',
      rawLbl: 'Raw message from the source',
      capRejectedNote: 'This request was rejected: {reason}',
      capTruncated:
        'This message was actually {size}, past the sample retention limit — only the opening part is kept. Enough to see the shape, but "Use as sample payload" will not parse it in full, and that is not a template problem.',
      sniffed: 'detected as {type}',
      useAsSample: 'Use as sample payload',
      usedAsSample: 'Copied into the sample payload',
      latestOnly: 'only the latest one is kept — it is the shared sample payload',
      capExpires: 'destroyed at {time}',
      capGone:
        'The previous sample passed its 3-hour lifetime and was destroyed — it holds full request headers and business fields, so no long-lived copy is kept. Start listening again to capture a new one.',
      title: 'Dry run · {name}',
      input: 'Sample payload',
      more: 'Headers and query (optional)',
      query: 'Query',
      headers: 'Headers',
      headersHint: 'One per line as "Name: value". Use headers.<name> in conditions.',
      run: 'Run',
      noSend: 'A dry run only resolves fields, conditions and templates — nothing is actually sent.',
      empty: 'Click "Run" to see which rules match, how the message renders, and where it would go.',
      needBody: 'Paste a sample payload first',
      matched: '{n} rule(s) matched',
      truncated: 'Payload too large, truncated',
      blockedTag: 'Blocked by keywords',
      blocked: 'This message would not get in: it fails the keyword gate',
      blockedHint:
        'Below is what would be sent if it passed; nothing is actually sent. Either have the source include one of the agreed words, or edit the list on the Security tab.',
      unresolved: 'These paths resolved to nothing',
      noMessage: 'No rule matched, so this push would send nothing.',
      noBranch:
        'Rule "{rules}" matched, but none of its output branch conditions held, so this push would send nothing.',
      noBranchHint:
        'The rule-level conditions already passed — what failed is the extra condition on the branch. Adjust the branch conditions against the field values above, or give the rule a catch-all branch with no conditions.',
      viaTemplate: 'Template: {name}',
      missingFields: '{n} field(s) resolved to nothing',
      sendTo: 'Send to',
      pushTitle: 'Chat-list preview',
      noTarget: 'No target selected, nothing would be sent',
      rootFields: 'Fields available in templates',
      rootHint: 'This is the structure templates actually see after the payload root path is applied.',
      rootDropped:
        'This message was too big — only its opening part was kept, so the field structure was not retained. To browse field names, have the source send a smaller one, or paste a sample above.',
    },
  },
  msg: {
    saveOk: 'Settings saved',
    wakeSent: 'Wake command sent',
    ddnsRun: 'DDNS executed',
    certIssued: 'Certificate operation done',
    passwordChanged: 'Password changed, please sign in again',
    uploadOk: 'Background uploaded',
    needRestart: 'Some settings require a restart',
    configExported: 'Configuration exported',
    configImported: 'Configuration imported',
    restartRequired: 'Some changes require a service restart',
    // The import replaced the administrator account with the one from the backup; the session is gone.
    importCredsChanged: 'The administrator account from the backup is now in effect — sign in again with those credentials',
    restarting: 'Settings updated — the service is restarting and will redirect to the new address…',
  },
}

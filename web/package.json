{
  "name": "mantou-web",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "description": "mantou 控制面板前端（Vue 3 + Vite + TypeScript）",
  "engines": {
    "node": ">=22.12.0"
  },
  "scripts": {
    "dev": "vite",
    "build": "node scripts/lint-i18n.mjs && vue-tsc --noEmit && vite build && node scripts/keep-dist.mjs",
    "build:only": "node scripts/lint-i18n.mjs && vite build && node scripts/keep-dist.mjs",
    "preview": "vite preview"
  },
  "dependencies": {
    "@element-plus/icons-vue": "^2.3.2",
    "axios": "^1.20.0",
    "echarts": "^6.1.0",
    "element-plus": "^2.14.5",
    "pinia": "^4.0.3",
    "vue": "^3.5.42",
    "vue-i18n": "^11.4.10",
    "vue-router": "^5.3.1"
  },
  "devDependencies": {
    "@types/node": "^24.13.3",
    "@vitejs/plugin-vue": "^6.0.8",
    "typescript": "^5.9.3",
    "vite": "^8.2.2",
    "vue-tsc": "^3.3.11"
  }
}
